# Reusable workflow: full FHIR IG build and GitHub Pages deployment.
#
# Split-checkout model
# ────────────────────
# This workflow separates *content* (the IG's FHIR Shorthand, pages, images,
# DMN files …) from the *scripts* that preprocess and postprocess around the
# IG Publisher.
#
#   • Content comes from the ref that triggered the event (or the explicit
#     `ref` input, e.g. a PR head SHA).
#   • Scripts are fetched from the smart-base repository at the ref given by
#     `scripts_ref` (default: main).
#
# This means feature-branch PRs only need to carry content changes.  The
# build tooling always comes from a known-good baseline (main), unless a
# trusted contributor explicitly overrides `scripts_ref` for testing new
# script code — which requires environment approval in the caller workflow.
#
# For downstream WHO SMART Guidelines IGs that call this workflow via
# workflow_call, scripts are downloaded on demand via curl when they are not
# already present locally.  The `scripts_ref` input controls which smart-base
# branch those downloads target.
#
# Make sure your repo has a branch called gh-pages.
#
# Change log:
# 2021-06-18: (JCT): publish default branches to / , other branches branches/<branch>
# 2021-11-26: (JCT): Reusable workflow
# 2022-01-28: (JCT): add auto-create gh-pages if it doesn't exist
# 2023-01-22: (JCT): use checkout action v3, and JamesIves/github-pages-deploy-action@v4
# 2026-03-xx: Remove direct push/PR triggers; add git safe.directory fix for Docker step.
# 2026-03-xx: Add split-checkout model with scripts_ref input.

name: FHIR IG Build and GitHub Pages Deployment

on:
  workflow_call: # Reusable by other workflows
    inputs:
      ref:
        description: 'Git ref to check out for content (e.g. PR head SHA).  When empty the default ref for the triggering event is used.'
        required: false
        type: string
      scripts_ref:
        description: 'Branch/ref of smart-base to fetch pre/post-processing scripts from (default: main).  Non-default values require approval in the caller workflow.'
        required: false
        type: string
        default: 'main'
      tx:
        required: false
        type: string
      do_dak:
        description: 'Enable DAK preprocessing and postprocessing'
        required: false
        type: boolean
        default: true
      deploy:
        description: 'Deploy the built IG to GitHub Pages'
        required: false
        type: boolean
        default: true

  # Allows you to run this workflow manually from the Actions tab
  # (useful for downstream repos and for ad-hoc smart-base builds).
  workflow_dispatch:
    inputs:
      scripts_ref:
        description: 'Branch/ref of smart-base to fetch scripts from (default: main)'
        required: false
        type: string
        default: 'main'
      tx:
        description: 'Optional Custom terminology server URL'
        required: false
      do_dak:
        description: 'Enable DAK preprocessing and postprocessing'
        required: false
        type: boolean
        default: true
      deploy:
        description: 'Deploy the built IG to GitHub Pages'
        required: false
        type: boolean
        default: true

# NOTE: No workflow-level concurrency block here.  When this workflow is
# invoked via workflow_call, github.workflow resolves to the *caller's*
# workflow name.  If both the caller (e.g. ci.yml) and this reusable
# workflow define the same concurrency group, GitHub detects a deadlock
# and cancels the run.  Callers are responsible for their own concurrency
# controls.  For standalone workflow_dispatch runs, concurrency is not
# typically needed because they are triggered manually and intentionally.

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    permissions:
      contents: write        # required for JamesIves/github-pages-deploy-action to push gh-pages
      pull-requests: write   # required for PR build-status comment steps
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Record checkout timestamp
        run: echo "TS_CHECKOUT=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV

      # ── Resolve the smart-base scripts source ──────────────────────────
      # Sets SCRIPTS_BASE_URL for curl-based downloads (downstream repos)
      # and SCRIPTS_REF for the git-based overlay (smart-base itself).
      - name: Resolve scripts source
        env:
          SCRIPTS_REF_INPUT: ${{ inputs.scripts_ref || 'main' }}
        run: |
          # Sanitize: allow only valid Git ref characters
          SCRIPTS_REF="$(printf '%s' "$SCRIPTS_REF_INPUT" | tr -cd 'a-zA-Z0-9._/-')"
          if [ -z "$SCRIPTS_REF" ]; then
            SCRIPTS_REF="main"
          fi
          SCRIPTS_BASE_URL="https://raw.githubusercontent.com/WorldHealthOrganization/smart-base/${SCRIPTS_REF}"
          printf 'SCRIPTS_REF=%s\n' "$SCRIPTS_REF"
          printf 'SCRIPTS_REF=%s\n' "$SCRIPTS_REF" >> "$GITHUB_ENV"
          printf 'SCRIPTS_BASE_URL=%s\n' "$SCRIPTS_BASE_URL"
          printf 'SCRIPTS_BASE_URL=%s\n' "$SCRIPTS_BASE_URL" >> "$GITHUB_ENV"

      # ── Overlay smart-base scripts onto content checkout ───────────────
      # When running inside the smart-base repository, the content checkout
      # may contain locally-modified copies of scripts.  This step replaces
      # them with the canonical versions from SCRIPTS_REF so that PR
      # branches only need to carry content, not build tooling.
      #
      # For downstream repos this step is a no-op — those repos don't have
      # input/scripts/ in their own tree and rely on the curl-based
      # fallback downloads in subsequent steps.
      - name: Overlay smart-base scripts
        env:
          GH_REPOSITORY: ${{ github.repository }}
        run: |
          # Only overlay when running inside the smart-base repository.
          SMART_BASE_REPO="WorldHealthOrganization/smart-base"
          if [[ "$GH_REPOSITORY" != "$SMART_BASE_REPO" ]]; then
            echo "Downstream repository — scripts will be fetched on demand"
            exit 0
          fi

          echo "Smart-base repo detected — overlaying scripts from ${SCRIPTS_REF}"
          git fetch origin "${SCRIPTS_REF}" --depth=1
          # Overlay input/scripts/ and supporting includes from the scripts ref.
          # This overwrites any locally-modified copies with the canonical versions.
          git checkout FETCH_HEAD -- input/scripts/
          git checkout FETCH_HEAD -- input/includes/dmn2html.xslt input/includes/dmn.css 2>/dev/null || true
          echo "✅ Scripts overlaid from ${SCRIPTS_REF}"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "GitPython>=3.1.40" "PyYAML>=6.0" "requests>=2.28.0" lxml

      - name: Find PR number
        id: find_pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_EVENT_NAME: ${{ github.event_name }}
          GH_EVENT_NUMBER: ${{ github.event.number }}
          GH_REF_NAME: ${{ github.ref_name }}
        run: python3 input/scripts/find_pr.py

      - name: Comment on PR - Build started
        if: steps.find_pr.outputs.IS_PR == 'true'
        env:
          PR_NUMBER: ${{ steps.find_pr.outputs.PR_NUMBER }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_RUN_ID: ${{ github.run_id }}
          GH_SHA: ${{ github.sha }}
          GH_BRANCH: ${{ github.head_ref || github.ref_name }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Check if PR comment script exists locally, download if needed
          if [ ! -f "input/scripts/pr_comment_start.py" ]; then
            echo "PR comment script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/pr_comment_start.py" "${SCRIPTS_BASE_URL}/input/scripts/pr_comment_start.py" 2>/dev/null || echo "Failed to download pr_comment_start.py"
          fi

          # Run PR comment script if available
          if [ -f "input/scripts/pr_comment_start.py" ]; then
            python3 input/scripts/pr_comment_start.py \
              "$PR_NUMBER" \
              "$GH_REPOSITORY" \
              "$GH_RUN_ID" \
              "$GH_SHA" \
              "$GH_BRANCH" \
              "$GH_TOKEN"
          else
            echo "⚠️ PR comment script not available, skipping build start comment"
          fi

      - name: Get branch name
        env:
          GH_HEAD_REF: ${{ github.head_ref }}
          GH_REF_NAME: ${{ github.ref_name }}
        run: python3 input/scripts/resolve_branch.py

      - name: Echo branch name and check if it's the default branch
        run: |
          echo "Current Branch: $BRANCH_NAME"
          DEFAULT_BRANCH=$(git remote show origin | sed -n '/HEAD branch/s/.*: //p')
          echo "Default Branch: $DEFAULT_BRANCH"
          if [ "$BRANCH_NAME" == "$DEFAULT_BRANCH" ]; then
            echo "This is the default branch."
            echo "IS_DEFAULT_BRANCH=true" >> $GITHUB_ENV
          else
            echo "This is NOT the default branch."
            echo "IS_DEFAULT_BRANCH=false" >> $GITHUB_ENV
          fi


      # - name: Create gh-pages branch if it doesn't exist
      #   run: |
      #     git fetch origin
      #     exists=`git show-ref refs/heads/gh-pages`
      #     if [ -n "$exists" ]; then
      #       echo 'gh-pages branch exists';
      #     else
      #       echo 'gh-pages branch does not exist, creating it';
      #       git checkout --orphan gh-pages
      #       git reset --hard
      #       git commit --allow-empty -m "Initializing gh-pages branch"
      #       git push origin gh-pages
      #       git checkout ${GITHUB_REF##*/}
      #     fi



      - name: Prepare DAK environment
        if: inputs.do_dak != 'false'
        run: |
          echo "Preparing DAK environment with configuration scripts..."

          # Check if DAK preparation scripts exist locally, download if needed
          if [ ! -f "input/scripts/generate_dak_from_sushi.py" ]; then
            echo "DAK generation script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_dak_from_sushi.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_dak_from_sushi.py" 2>/dev/null || echo "Failed to download generate_dak_from_sushi.py"
          fi

          if [ ! -f "input/scripts/update_sushi_config.py" ]; then
            echo "Sushi config update script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/update_sushi_config.py" "${SCRIPTS_BASE_URL}/input/scripts/update_sushi_config.py" 2>/dev/null || echo "Failed to download update_sushi_config.py"
          fi

          # First, generate DAK configuration from sushi config (only if smart.who.int.base is a dependency and dak.json doesn't exist)
          if [ -f "input/scripts/generate_dak_from_sushi.py" ]; then
            echo "Running generate_dak_from_sushi.py..."
            python3 input/scripts/generate_dak_from_sushi.py
            echo "✅ DAK configuration processing completed"
          else
            echo "⚠️ generate_dak_from_sushi.py not available, skipping DAK generation"
          fi

          # Then, update sushi config with DAK API settings (checks if dak.json is present)
          if [ -f "input/scripts/update_sushi_config.py" ]; then
            echo "Running update_sushi_config.py..."
            python3 input/scripts/update_sushi_config.py
            echo "✅ Sushi configuration updated for DAK API"
          else
            echo "⚠️ update_sushi_config.py not available, skipping sushi config update"
          fi

      - name: Check for DAK configuration
        env:
          DO_DAK: ${{ inputs.do_dak }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_REF_NAME: ${{ github.head_ref || github.ref_name }}
        run: |
          if [ "$DO_DAK" == "false" ]; then
            echo "Not generating DAK"
            exit 0
          fi
          echo "DAK processing enabled, checking for dak.json..."
          if [ -f "dak.json" ]; then
            echo "✅ Found dak.json - DAK processing will be enabled"
            echo "DAK_ENABLED=true" >> "$GITHUB_ENV"

            # Set repository context for DAK URL generation
            echo "GITHUB_REPOSITORY=${GH_REPOSITORY}" >> "$GITHUB_ENV"
            echo "GITHUB_REF_NAME=${GH_REF_NAME}" >> "$GITHUB_ENV"
            echo "BRANCH_NAME=${GITHUB_REF#refs/heads/}" >> "$GITHUB_ENV"

          else
            echo "⚠️ do_dak=true but no dak.json found in repository root"
            echo "DAK processing will be skipped"
            echo "DAK_ENABLED=false" >> "$GITHUB_ENV"
          fi

      - name: DAK Preprocessing - Regenerate DAK configuration with current context
        if: (inputs.do_dak != 'false') && (env.DAK_ENABLED == 'true')
        run: |
          echo "Regenerating DAK configuration with current branch context..."

          # Check if DAK generation script exists locally, download if needed
          if [ ! -f "input/scripts/generate_dak_from_sushi.py" ]; then
            echo "DAK generation script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_dak_from_sushi.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_dak_from_sushi.py" 2>/dev/null || echo "Failed to download generate_dak_from_sushi.py"
            curl -L -f -o "input/scripts/dak_url_utils.py" "${SCRIPTS_BASE_URL}/input/scripts/dak_url_utils.py" 2>/dev/null || echo "Failed to download dak_url_utils.py"
          fi

          # Regenerate DAK configuration with current environment context
          if [ -f "input/scripts/generate_dak_from_sushi.py" ]; then
            python3 input/scripts/generate_dak_from_sushi.py
            echo "✅ DAK configuration regenerated with current context"
          else
            echo "⚠️ DAK generation script not available, using existing dak.json"
          fi

      - name: DAK Preprocessing - Generate DMN Questionnaires
        if: (inputs.do_dak != 'false') && (env.DAK_ENABLED == 'true')
        run: |
          echo "Generating FHIR Questionnaires from DMN files before IG publisher runs..."

          # Check if questionnaire generation files exist locally
          if [ ! -f "input/scripts/dmn_questionnaire_generator.py" ]; then
            echo "DMN questionnaire generator not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts

            # Download questionnaire generator from smart-base repository
            if curl -L -f -o "input/scripts/dmn_questionnaire_generator.py" "${SCRIPTS_BASE_URL}/input/scripts/dmn_questionnaire_generator.py" 2>/dev/null; then
              echo "Downloaded questionnaire generator from main branch"
            else
              echo "Failed to download questionnaire generator, skipping questionnaire generation"
            fi
          else
            echo "Using local DMN questionnaire generator"
          fi

          # Run questionnaire generation if script is available
          if [ -f "input/scripts/dmn_questionnaire_generator.py" ]; then
            python3 input/scripts/dmn_questionnaire_generator.py
            echo "✅ Questionnaires generated successfully"
          else
            echo "⚠️ DMN questionnaire generator not available, skipping questionnaire generation"
          fi

      - name: DAK Preprocessing - Transform DMN files to HTML
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Transforming DMN files to HTML before IG publisher runs..."

          # Check if DMN transformation files exist locally
          if [ ! -f "input/scripts/transform_dmn.py" ]; then
            echo "DMN transformation files not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts input/includes

            # Download transformation files from smart-base repository
            curl -L -f -o "input/scripts/transform_dmn.py" "${SCRIPTS_BASE_URL}/input/scripts/transform_dmn.py" 2>/dev/null || echo "Failed to download transform_dmn.py"
            curl -L -f -o "input/includes/dmn2html.xslt" "${SCRIPTS_BASE_URL}/input/includes/dmn2html.xslt" 2>/dev/null || echo "Failed to download dmn2html.xslt"
            curl -L -f -o "input/includes/dmn.css" "${SCRIPTS_BASE_URL}/input/includes/dmn.css" 2>/dev/null || echo "Failed to download dmn.css"
          else
            echo "Using local DMN transformation files"
          fi

          # Run DMN transformation if script is available
          if [ -f "input/scripts/transform_dmn.py" ]; then
            python3 input/scripts/transform_dmn.py
            echo "✅ DMN files transformed successfully"
          else
            echo "⚠️ DMN transformation script not available, skipping DMN transformation"
          fi

      - name: DAK Preprocessing - Inject diagram translations
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Injecting translations into diagram sources before IG publisher runs..."

          # Check if injection script exists locally, download if needed
          if [ ! -f "input/scripts/inject_translations.py" ]; then
            echo "Translation injection script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/inject_translations.py" \
              "${SCRIPTS_BASE_URL}/input/scripts/inject_translations.py" \
              2>/dev/null || echo "Failed to download inject_translations.py"
          else
            echo "Using local inject_translations.py"
          fi

          # Run injection if script is available
          if [ -f "input/scripts/inject_translations.py" ]; then
            python3 input/scripts/inject_translations.py
            echo "✅ Diagram translations injected successfully"
          else
            echo "⚠️ Translation injection script not available, skipping"
          fi

      - name: Update the image to the latest publisher
        uses: docker://hl7fhir/ig-publisher-base:latest
        with:
          # Get the latest publisher - don't run the batch script but run the line directly
          args: curl -L https://github.com/HL7/fhir-ig-publisher/releases/latest/download/publisher.jar -o ./input-cache/publisher.jar --create-dirs


      - name: Create package cache folder
        uses: docker://hl7fhir/ig-publisher-base:latest
        with:
          entrypoint: /bin/sh
          args: -c "mkdir -p ./fhir-package-cache && chown 1001:127 ./fhir-package-cache"


      - name: Run the IG publisher
        uses: docker://hl7fhir/ig-publisher-base:latest
        with:
          entrypoint: /bin/sh
          args: -c "mkdir -p /var/lib/.fhir && chown $(id -u):$(id -g) /var/lib/.fhir"

      - name: Run the IG publisher with optional tx
        env:
          INPUT_TX: ${{ inputs.tx }}
          GH_WORKSPACE: ${{ github.workspace }}
        run: |
          echo "TX input: $INPUT_TX"

          CMD="java -Xmx6g -jar ./input-cache/publisher.jar publisher \
            -ig . \
            -auto-ig-build \
            -repo https://github.com/${GITHUB_REPOSITORY}/tree/${GITHUB_REF_NAME} \
            -package-cache-folder ./fhir-package-cache"

          if [ -n "$INPUT_TX" ]; then
            CMD="$CMD -tx $INPUT_TX"
          fi

          echo "Running command: $CMD"

          # Start container with entrypoint (sets up sushi, jekyll, etc.) and keep it running
          docker run -d --name ig-run \
            -v "${GH_WORKSPACE}":/work \
            -w /work \
            hl7fhir/ig-publisher-base:latest \
            sleep infinity

          # Wait for entrypoint to finish setup
          sleep 5

          # Mark /work as a safe directory to suppress git dubious ownership warnings
          docker exec ig-run git config --global --add safe.directory /work

          # Install Python and SUSHI as root
          docker exec --user root ig-run sh -c '\
            apt-get update && \
            apt-get install -y --no-install-recommends python3 python3-pip python3-venv && \
            ln -sf /usr/bin/python3 /usr/bin/python && \
            pip3 install --break-system-packages "GitPython>=3.1.40" "PyYAML>=6.0" "requests>=2.28.0" lxml && \
            npm install -g fsh-sushi && \
            sushi --version'

          # Run publisher as the default user
          docker exec -w /work ig-run sh -c "$CMD"

          # Cleanup
          docker rm -f ig-run

          echo "TS_IG_BUILD=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV

      # Additional step to upload qa.json as an artifact
      - name: Upload qa.json artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: qa-json-artifact
          path: ./output/qa.json  # Adjust the path based on where qa.json is located

      # The IG Publisher writes translation files to two locations:
      #   1. output/*.pot  — translation templates (gitignored)
      #   2. translations/{lang}/po/*.po — per-resource per-language .po files
      #      (also gitignored via /translations in .gitignore)
      # Copy .pot files from output/ and merge per-resource .po files from
      # translations/ into a single base.pot for Weblate integration.
      - name: Collect IG Publisher translation files
        if: success()
        run: |
          mkdir -p input/translations
          found=0

          # Copy .pot files from output/
          for pot in $(find output/ -name '*.pot' 2>/dev/null); do
            echo "Found IG Publisher .pot: $pot"
            cp "$pot" input/translations/
            found=$((found + 1))
          done

          # Count .po files from translations/ (IG Publisher i18n output)
          for po in $(find translations/ -name '*.po' 2>/dev/null); do
            echo "Found IG Publisher .po: $po"
            found=$((found + 1))
          done

          echo "Found $found translation file(s) from IG Publisher"

          # Merge per-resource .po files into base.pot using the Python helper.
          if [ -f "input/scripts/run_ig_publisher.py" ]; then
            python3 -c "
          import sys, os
          sys.path.insert(0, os.path.join('.', 'input', 'scripts'))
          from run_ig_publisher import collect_publisher_pot_files
          collect_publisher_pot_files('.')
          " 2>&1 || echo "Warning: Python merge step failed; .pot files from output/ still collected"
          fi

      # Upload translation template (.pot) files produced by the IG Publisher
      # so that downstream jobs (e.g. commit-pot) can reuse them without
      # re-running the publisher.
      - name: Upload POT file artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: pot-files
          path: |
            input/translations/**/*.pot
            input/fsh/translations/**/*.pot
            input/images-source/translations/**/*.pot
            input/images/translations/**/*.pot
            input/archimate/translations/**/*.pot
            input/diagrams/translations/**/*.pot
            input/pagecontent/translations/**/*.pot
          if-no-files-found: ignore
          retention-days: 1

      - name: Fix output directory ownership for DAK post-processing
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          # The IG publisher runs in Docker as root, so output/ files are root-owned.
          # Python post-processing scripts run as the GitHub Actions runner (non-root)
          # and must be able to write schema files and modify HTML pages.
          sudo chown -R "$USER:$USER" ./output || true

      - name: DAK Postprocessing - Generate JSON Schemas
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Generating JSON schemas from FHIR resources..."

          # Check if schema generation files exist locally, download if needed
          if [ ! -f "input/scripts/generate_logical_model_schemas.py" ]; then
            echo "Schema generation files not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_logical_model_schemas.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_logical_model_schemas.py" 2>/dev/null || echo "Failed to download logical model schema generator"
            curl -L -f -o "input/scripts/generate_valueset_schemas.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_valueset_schemas.py" 2>/dev/null || echo "Failed to download valueset schema generator"
          fi

          # Generate logical model schemas
          if [ -f "input/scripts/generate_logical_model_schemas.py" ]; then
            python3 input/scripts/generate_logical_model_schemas.py
            echo "✅ Logical model schemas generated"
          fi

          # Generate valueset schemas
          if [ -f "input/scripts/generate_valueset_schemas.py" ]; then
            python3 input/scripts/generate_valueset_schemas.py
            echo "✅ ValueSet schemas generated"
          fi

      - name: DAK Postprocessing - Generate JSON-LD Vocabularies
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Generating JSON-LD vocabularies from ValueSet expansions..."

          # Check if JSON-LD generation files exist locally, download if needed
          if [ ! -f "input/scripts/generate_jsonld_vocabularies.py" ]; then
            echo "JSON-LD vocabulary generator not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_jsonld_vocabularies.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_jsonld_vocabularies.py" 2>/dev/null || echo "Failed to download JSON-LD vocabulary generator"
          fi

          # Generate JSON-LD vocabularies
          if [ -f "input/scripts/generate_jsonld_vocabularies.py" ]; then
            python3 input/scripts/generate_jsonld_vocabularies.py
            echo "✅ JSON-LD vocabularies generated"
          else
            echo "⚠️ JSON-LD vocabulary generator not available, skipping vocabulary generation"
          fi

      - name: DAK Postprocessing - Generate DAK API Hub
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Generating DAK API documentation hub..."

          # Check if API hub generation files exist locally, download if needed
          if [ ! -f "input/scripts/generate_dak_api_hub.py" ]; then
            echo "DAK API hub generator not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_dak_api_hub.py" "${SCRIPTS_BASE_URL}/input/scripts/generate_dak_api_hub.py" 2>/dev/null || echo "Failed to download DAK API hub generator"
          fi

          # Generate DAK API hub
          if [ -f "input/scripts/generate_dak_api_hub.py" ]; then
            python3 input/scripts/generate_dak_api_hub.py
            echo "✅ DAK API hub generated"
          else
            echo "⚠️ DAK API hub generator not available, skipping API hub generation"
          fi

      - name: DAK Postprocessing - Update translated image references
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Updating image references in translated HTML pages..."

          # Check if the script exists locally, download if needed
          if [ ! -f "input/scripts/update_translated_image_refs.py" ]; then
            echo "update_translated_image_refs.py not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/update_translated_image_refs.py" \
              "${SCRIPTS_BASE_URL}/input/scripts/update_translated_image_refs.py" \
              2>/dev/null || echo "Failed to download update_translated_image_refs.py"
          else
            echo "Using local update_translated_image_refs.py"
          fi

          # Run the reference updater if available
          if [ -f "input/scripts/update_translated_image_refs.py" ]; then
            python3 input/scripts/update_translated_image_refs.py
            echo "✅ Translated image references updated"
          else
            echo "⚠️ update_translated_image_refs.py not available, skipping"
          fi


      - name: DAK Postprocessing - Generate smart.liquid variables
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Generating smart.liquid Liquid variable include from IG Publisher output..."

          # Check if the script exists locally, download if needed
          if [ ! -f "input/scripts/generate_smart_liquid.py" ]; then
            echo "generate_smart_liquid.py not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/generate_smart_liquid.py" \
              "${SCRIPTS_BASE_URL}/input/scripts/generate_smart_liquid.py" \
              2>/dev/null || echo "Failed to download generate_smart_liquid.py"
          else
            echo "Using local generate_smart_liquid.py"
          fi

          # Run the generator if available
          if [ -f "input/scripts/generate_smart_liquid.py" ]; then
            python3 input/scripts/generate_smart_liquid.py
            echo "✅ smart.liquid variables generated"
          else
            echo "⚠️ generate_smart_liquid.py not available, skipping"
          fi

      - name: DAK Postprocessing - Inject smart.liquid include into markdown pages
        if: inputs.do_dak != 'false' && env.DAK_ENABLED == 'true'
        run: |
          echo "Injecting {% include smart.liquid %} into DAK IG markdown pages..."

          # Check if the script exists locally, download if needed
          if [ ! -f "input/scripts/inject_smart_liquid.py" ]; then
            echo "inject_smart_liquid.py not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/inject_smart_liquid.py" \
              "${SCRIPTS_BASE_URL}/input/scripts/inject_smart_liquid.py" \
              2>/dev/null || echo "Failed to download inject_smart_liquid.py"
          else
            echo "Using local inject_smart_liquid.py"
          fi

          # Run the injector if available
          if [ -f "input/scripts/inject_smart_liquid.py" ]; then
            python3 input/scripts/inject_smart_liquid.py
            echo "✅ smart.liquid include injected into markdown pages"
          else
            echo "⚠️ inject_smart_liquid.py not available, skipping"
          fi

      - name: Stamp deploy metadata
        if: env.DAK_ENABLED == 'true'
        env:
          GH_SHA: ${{ github.sha }}
        run: python3 input/scripts/stamp_deploy.py output/index.html


      # ── Upload large package files as GitHub Release assets ────────
      # Instead of deploying package.db, package.tgz, package.r4.tgz,
      # package.r4b.tgz, and ai.zip to gh-pages (where they bloat the
      # branch), upload them as binary assets on a GitHub Release tagged
      # with the IG version from sushi-config.yaml.
      - name: Upload FHIR package assets as GitHub Release
        if: success()
        id: package_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          # Check if release script exists locally, download if needed
          if [ ! -f "input/scripts/create_package_release.py" ]; then
            echo "Release script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/create_package_release.py" \
              "${SCRIPTS_BASE_URL}/input/scripts/create_package_release.py" \
              2>/dev/null || echo "Failed to download create_package_release.py"
          fi

          if [ -f "input/scripts/create_package_release.py" ]; then
            python3 input/scripts/create_package_release.py --output-dir ./output
            echo "✅ Package release created"
          else
            echo "⚠️ create_package_release.py not available, skipping package release"
          fi

      - name: Delete files >100MB before deployment
        run: |
          echo "Removing files over 100 MB from ./output..."
          find ./output -type f -size +100M -print -delete

      - name: Deploy candidate
        uses: JamesIves/github-pages-deploy-action@v4.4.2
        if: env.IS_DEFAULT_BRANCH == 'false' && inputs.deploy != false
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: ./output # The folder the action should deploy.
          commit-message: Deploy candidate branch
          target-folder: branches/${{ env.BRANCH_DIR }}
          single-commit: true
          clean: false

      - name: Deploy main
        uses: JamesIves/github-pages-deploy-action@v4.4.2
        if: env.IS_DEFAULT_BRANCH == 'true' && inputs.deploy != false
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: ./output # The folder the action should deploy.
          commit-message: Deploy main branch
          single-commit: true
          clean-exclude: |
            branches
            sitepreview

      - name: Comment on PR - Deployment completed
        if: steps.find_pr.outputs.IS_PR == 'true' && (success() || failure())
        env:
          PR_NUMBER: ${{ steps.find_pr.outputs.PR_NUMBER }}
          GH_REPOSITORY: ${{ github.repository }}
          GH_RUN_ID: ${{ github.run_id }}
          GH_SHA: ${{ github.sha }}
          GH_BRANCH: ${{ github.head_ref || github.ref_name }}
          JOB_STATUS: ${{ job.status }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Check if PR comment script exists locally, download if needed
          if [ ! -f "input/scripts/pr_comment_finish.py" ]; then
            echo "PR comment script not found locally, downloading from smart-base repository..."
            mkdir -p input/scripts
            curl -L -f -o "input/scripts/pr_comment_finish.py" "${SCRIPTS_BASE_URL}/input/scripts/pr_comment_finish.py" 2>/dev/null || echo "Failed to download pr_comment_finish.py"
          fi

          # Run PR comment script if available
          if [ -f "input/scripts/pr_comment_finish.py" ]; then
            python3 input/scripts/pr_comment_finish.py \
              "$PR_NUMBER" \
              "$GH_REPOSITORY" \
              "$GH_RUN_ID" \
              "$GH_SHA" \
              "$GH_BRANCH" \
              "$JOB_STATUS" \
              "$GH_TOKEN"
          else
            echo "⚠️ PR comment script not available, skipping deployment complete comment"
          fi
